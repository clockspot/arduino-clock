// This sketch's source code lives in src/main.cpp, organized for compatibility
// with both the Arduino IDE and PlatformIO. This .ino file is intentionally
// empty; the Arduino IDE only requires its presence (with a name matching the
// folder) to recognize the directory as a sketch.
